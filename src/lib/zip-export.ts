/**
 * zip-export.ts — Folder zip export
 * Full implementation in task 20. This stub satisfies the import so the build compiles.
 */

export async function exportFolderAsZip(
  _folderId: string,
  _format: 'markdown' | 'pdf',
): Promise<Blob> {
  throw new Error('exportFolderAsZip: not yet implemented (task 20)');
}
